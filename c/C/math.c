#include <stdio.h>
#include <math.h>

int main()
{
    printf("%f\n", sqrt(16)); // find the square root

    printf("%f\n", ceil(1.4));  // upwards
    printf("%f\n", floor(1.4)); // downwards

    printf("%f \n", pow(4, 3)); // return the value of x to the power of y(xy)
    return 0;
}